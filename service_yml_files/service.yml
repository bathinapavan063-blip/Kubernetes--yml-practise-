apiVersion: v1
kind: Service
metadata:
 name: nginx-service
spec:
 type: NodePort
 ports:
  - name: http
    protocol: TCP
    targetPort: 80   
    port: 80         
    nodePort: 30000  
 selector:
  name: web-app
  type: frontend 
